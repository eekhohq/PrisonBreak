using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestGen : TerrainConfig
{
    /*public Material mat;

    public Vector2Int size;
    public float scale;
    [Range(1, 10)]
    public int octaves;
    [Range(1f, 3f)]
    public float lacunarity;
    [Range(0f, 1f)]
    public float persistance;
    public Vector3 offset;

    // Start is called before the first frame update
    void Start()
    {
        mat = gameObject.GetComponent<MeshRenderer>().sharedMaterial;

        mat.mainTexture = ProceduralUtils.GenerateTexture2D(ProceduralUtils.GenerateTerrainData(size.x, size.x, scale/1000, octaves, lacunarity, persistance, offset));
        mat.mainTexture.filterMode = FilterMode.Point;
        mat.mainTexture.wrapMode = TextureWrapMode.Clamp;
    }

    private void OnValidate()
    {
        mat.mainTexture = ProceduralUtils.GenerateTexture2D(ProceduralUtils.GenerateTerrainData(size.x, size.x, scale/1000, octaves, lacunarity, persistance, offset));
    }*/
}
